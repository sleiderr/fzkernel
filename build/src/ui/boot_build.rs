use ratatui::widgets::Widget;

pub struct BootloaderBuild {}

impl Widget for BootloaderBuild {
    fn render(self, area: ratatui::prelude::Rect, buf: &mut ratatui::prelude::Buffer) {
        todo!()
    }
}
